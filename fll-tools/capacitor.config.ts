import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ev3lessons.flltools',
  appName: 'FLL Tools',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
